{
  "name": "@enfurl/edge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "furl.li edge worker: GET /<furl> unfurls it into a 302, /<furl>+ shows where it leads, everything else is the static app. Keeps nothing.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/worker.ts --bundle --format=esm --outfile=dist/worker.js --minify"
  },
  "dependencies": {
    "@enfurl/codec": "0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
